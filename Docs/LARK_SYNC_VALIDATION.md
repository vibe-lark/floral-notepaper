# 飞书同步验收记录

日期：2026-09-20（北京时间）。

## 2026-09-22：Mac 快捷键与单链接配置

- 同步设置仅保留一个链接输入框，新增原生链接解析、表选择校验及 CLI 自动查找；兼容原来的连接配置。
- 生产 Rust 链接解析代码已对真实 Base 的带表链接和单表 Base 链接完成只读验证，字段校验通过；没有写入云端记录。
- 新增快捷键测试覆盖 Mac Command 组合、Control/Unix 编辑键透传、组合输入、重复按键、精确修饰键和非 Mac 兼容行为。
- 新增链接测试覆盖域名白名单、URL 凭证拒绝、多表不误选、非表格 block 拒绝、旧配置保留，以及 CLI 授权/字段校验失败后不覆盖现有连接。
- 前端构建通过，仍有上游缺失的粗体字体资源警告；原生完整编译仍受本机 GLib/GTK/WebKit 缺失限制。快捷键的实际 Mac 窗口交互与新版安装包尚未验收。

## 已通过

- `npm ci`：安装成功，无 npm audit 漏洞；上游 lint-staged 有 Node 最低版本警告。
- `npm run build` / TypeScript：通过；上游未入库的 SourceHanSerifSC-Bold 字体有资源警告，不属于本次新增。
- `npm test`：25 个测试文件、114 项测试通过，其中4项为新增同步接口/设置状态测试。
- `npm run test:sync`：28 项通过，其中13项为同步测试，其余为直接复用的原笔记存储测试。
- `npm run lint`、脚本语法检查、`git diff --check`：通过。
- `npm run test:sync:live`：直接使用生产 Rust 存储/同步源码，经 Lark CLI 完成真实 Base 验收。
- `npm run sync:check`：真实字段校验通过；首轮验收后3条记录，其中1条使用指南可见、2条验收记录软删除。
- `npm run sync:once`：新的隔离本地目录成功拉取使用指南便签。

真实验收覆盖：字段契约、中文/emoji/代码块 Markdown 往返、第二设备拉取、重复同步幂等、直接改云端后回传、两端修改保留冲突副本、软删除、取消删除恢复。机器可读结果在 `.local/live-regression/verification.json`。测试记录不硬删，正文保留用于复核。

本地同步测试另覆盖：断网时保留草稿、损坏状态拒绝重置、路径穿越拒绝、缺字段/异常矩阵拒绝、云端物理删行拒绝、切换目标拒绝、未保存编辑保护、本地文件丢失不传播删除、数据目录迁移携带同步进度。

## 未完成的验证

- 原生桌面完整编译：`cargo check --manifest-path src-tauri/Cargo.toml --locked` 失败于系统 `glib-2.0 >= 2.70` 缺失；GTK3/WebKit2GTK 4.1 也未安装。失败发生在系统依赖阶段，不能据此声称 Tauri 集成代码已通过完整编译。
- 原生窗口启动、小窗/磁贴交互、托盘定时同步与关闭窗口行为：尚未在图形桌面实测。
- Windows/macOS/Linux 安装包：尚未生成和验收。
- 没有进行多用户账号权限矩阵或同一时刻多写者压力测试；当前实现定位为个人多设备同步，Base API 无原子条件更新的限制见使用说明。

结论：前端构建、独立同步核心与真实多维表格链路通过；完整桌面交付仍需在具备 Tauri 系统依赖的机器上编译及交互验收。
